# SUScape评估指标详细说明

## 概述

SUScape数据集的评估包含三个核心指标，用于评估自动驾驶模型的轨迹预测和安全性能力：

1. **L2 Trajectory Error（L2轨迹误差）**：测量预测轨迹与真实轨迹的平均欧氏距离
2. **Object Collision Rate（物体碰撞率）**：预测轨迹中心点与其他物体碰撞的比例
3. **Bounding Box Collision Rate（边界框碰撞率）**：预测车辆边界框与其他物体碰撞的比例

所有指标都在三个时间点计算：**1秒（2帧）、2秒（4帧）、3秒（6帧）**。

---

## 1. L2 Trajectory Error（L2轨迹误差）

### 定义

L2轨迹误差（也称为ADE - Average Displacement Error）衡量预测轨迹与真实轨迹之间的平均欧氏距离。

### 计算公式

```
L2 = (1/N) * Σ sqrt((pred_x[i] - gt_x[i])² + (pred_y[i] - gt_y[i])²)
```

其中：
- `N`: 预测的时间步数（1s=2帧，2s=4帧，3s=6帧）
- `pred_x[i], pred_y[i]`: 第i帧的预测位置
- `gt_x[i], gt_y[i]`: 第i帧的真实位置

### 代码实现

**文件**: `mmcv/models/dense_heads/planning_head_plugin/metric_stp3.py`

```python
def compute_L2(self, trajs, gt_trajs):
    '''
    trajs: torch.Tensor (n_future, 2)
    gt_trajs: torch.Tensor (n_future, 2)
    '''
    pred_len = trajs.shape[0]
    ade = float(
        sum(
            torch.sqrt(
                (trajs[i, 0] - gt_trajs[i, 0]) ** 2
                + (trajs[i, 1] - gt_trajs[i, 1]) ** 2
            )
            for i in range(pred_len)
        )
        / pred_len
    )
    
    return ade
```

**调用位置**: `mmcv/models/detectors/orion.py` (Line 1050-1053)

```python
traj_L2 = self.planning_metric.compute_L2(
    pred_ego_fut_trajs[0, :cur_time].detach().to(gt_ego_fut_trajs.device),
    gt_ego_fut_trajs[0, :cur_time]
)
metric_dict['plan_L2_{}s'.format(i+1)] = np.nan_to_num(traj_L2)
```

### 单位和含义

- **单位**: 米（meters）
- **含义**: 预测轨迹平均偏离真实轨迹的距离
- **越小越好**: 0表示完美预测

### 示例

```python
# 1秒预测（2帧）
预测轨迹: [(1.0, 0.5), (2.0, 1.0)]
真实轨迹: [(1.2, 0.6), (2.1, 0.9)]

# 计算
距离1 = sqrt((1.0-1.2)² + (0.5-0.6)²) = sqrt(0.04 + 0.01) = 0.224米
距离2 = sqrt((2.0-2.1)² + (1.0-0.9)²) = sqrt(0.01 + 0.01) = 0.141米

L2 = (0.224 + 0.141) / 2 = 0.183米
```

---

## 2. Object Collision Rate（物体碰撞率）

### 定义

物体碰撞率测量**预测轨迹的中心点**与场景中其他物体（车辆、行人）发生碰撞的比例。

### 计算方法

1. **构建占用栅格图（Occupancy Grid）**
   - 将所有其他车辆和行人投影到BEV（Bird's Eye View）栅格图上
   - 栅格分辨率：0.5米 × 0.5米
   - 范围：[-50m, 50m] × [-50m, 50m]

2. **检测碰撞**
   - 将预测轨迹点转换到栅格坐标
   - 检查该栅格位置是否被其他物体占用
   - 如果占用 → 碰撞

3. **计算碰撞率**
   ```
   Collision Rate = (碰撞帧数) / (总有效帧数)
   ```

### 代码实现

**文件**: `mmcv/models/dense_heads/planning_head_plugin/metric_stp3.py`

#### 步骤1: 构建占用栅格

```python
def get_birds_eye_view_label(self, gt_agent_boxes, gt_agent_feats):
    '''
    将GT车辆和行人投影到BEV栅格图
    '''
    T = 6  # 6帧（3秒）
    segmentation = np.zeros((T, self.bev_dimension[0], self.bev_dimension[1]))
    pedestrian = np.zeros((T, self.bev_dimension[0], self.bev_dimension[1]))
    
    # 对每个时间步和每个物体
    for t in range(T):
        for i in range(agent_num):
            if gt_agent_fut_mask[i][t] == 1:
                # 获取物体在t时刻的位置、朝向、尺寸
                x_a = gt_agent_fut_trajs[i, t, 0]
                y_a = gt_agent_fut_trajs[i, t, 1]
                yaw_a = gt_agent_fut_yaw[i, t, 0]
                
                # 计算物体占用的栅格区域并填充
                poly_region = self._get_poly_region_in_image(param)
                if (category_index in self.category_index['vehicle']):
                    cv2.fillPoly(segmentation[t], [poly_region], 1.0)
                if (category_index in self.category_index['human']):
                    cv2.fillPoly(pedestrian[t], [poly_region], 1.0)
    
    return segmentation, pedestrian
```

#### 步骤2: 碰撞检测

```python
def evaluate_coll(self, trajs, gt_trajs, segmentation):
    '''
    trajs: torch.Tensor (B, n_future, 2) - 预测轨迹
    gt_trajs: torch.Tensor (B, n_future, 2) - 真实轨迹
    segmentation: torch.Tensor (B, n_future, 200, 200) - 占用栅格
    '''
    obj_coll_sum = torch.zeros(n_future, device=segmentation.device)
    
    for i in range(B):
        # 检测真实轨迹的碰撞（用于过滤）
        gt_box_coll = self.evaluate_single_coll(gt_trajs[i], segmentation[i])
        
        # 将预测轨迹转换为栅格坐标
        xx, yy = trajs[i,:,0], trajs[i, :, 1]
        xi = ((-self.bx[0]/2 - yy) / self.dx[0]).long()
        yi = ((-self.bx[1]/2 + xx) / self.dx[1]).long()
        
        # 检查栅格位置是否被占用（排除GT碰撞的帧）
        m1 = torch.logical_and(
            torch.logical_and(xi >= 0, xi < self.bev_dimension[0]),
            torch.logical_and(yi >= 0, yi < self.bev_dimension[1]),
        )
        m1 = torch.logical_and(m1, torch.logical_not(gt_box_coll))
        
        # 统计碰撞
        obj_coll_sum[ti[m1]] += segmentation[i, ti[m1], xi[m1], yi[m1]].long()
    
    return obj_coll_sum
```

**调用位置**: `mmcv/models/detectors/orion.py` (Line 1054-1060)

```python
obj_coll, obj_box_coll = self.planning_metric.evaluate_coll(
    pred_ego_fut_trajs[:, :cur_time].detach(),
    gt_ego_fut_trajs[:, :cur_time],
    occupancy
)
metric_dict['plan_obj_col_{}s'.format(i+1)] = np.nan_to_num(obj_coll.mean().item())
```

### 特殊处理

**排除GT碰撞场景**：
- 如果真实轨迹本身就与某个物体碰撞，该帧不计入评估
- 这样可以避免惩罚模型在不可避免的碰撞场景中的表现

### 单位和含义

- **单位**: 比例（0-1之间）或百分比（0-100%）
- **含义**: 预测轨迹中心点发生碰撞的帧数占比
- **越小越好**: 0表示无碰撞，1表示所有帧都碰撞

### 示例

```python
# 1秒预测（2帧）
总帧数: 2
碰撞帧数: 0

Collision Rate = 0 / 2 = 0.0 (0%)

# 2秒预测（4帧）
总帧数: 4
碰撞帧数: 1

Collision Rate = 1 / 4 = 0.25 (25%)
```

---

## 3. Bounding Box Collision Rate（边界框碰撞率）

### 定义

边界框碰撞率测量**预测车辆的完整边界框**（考虑车辆尺寸）与场景中其他物体发生碰撞的比例。

### 与Object Collision Rate的区别

| 指标 | 碰撞检测方式 | 更严格 |
|------|-------------|-------|
| **Object Collision** | 仅检查轨迹**中心点** | 否 |
| **Bounding Box Collision** | 检查**整个车辆边界框** | 是 |

边界框碰撞率更严格，因为它考虑了车辆的实际尺寸（4.084m × 1.85m）。

### 计算方法

1. **构建自车边界框**
   ```python
   ego_width = 1.85m   # 车宽
   ego_length = 4.084m # 车长
   
   # 车辆四个角点（相对于中心）
   corners = [
       [-length/2, width/2],   # 左前
       [length/2, width/2],    # 右前
       [length/2, -width/2],   # 右后
       [-length/2, -width/2]   # 左后
   ]
   ```

2. **投影到栅格**
   - 对每个预测轨迹点，计算车辆四个角点的栅格坐标
   - 填充车辆占用的所有栅格

3. **检测碰撞**
   - 检查车辆占用的任何栅格是否与其他物体重叠
   - 如果重叠 → 边界框碰撞

4. **计算碰撞率**
   ```
   BBox Collision Rate = (边界框碰撞帧数) / (总有效帧数)
   ```

### 代码实现

**文件**: `mmcv/models/dense_heads/planning_head_plugin/metric_stp3.py`

```python
def evaluate_single_coll(self, traj, segmentation, input_gt):
    '''
    traj: torch.Tensor (n_future, 2) - 单个样本的轨迹
    segmentation: torch.Tensor (n_future, 200, 200) - 占用栅格
    '''
    # 定义自车的四个角点
    pts = np.array([
        [-self.H / 2. + 0.5, self.W / 2.],   # H=4.084m, W=1.85m
        [self.H / 2. + 0.5, self.W / 2.],
        [self.H / 2. + 0.5, -self.W / 2.],
        [-self.H / 2. + 0.5, -self.W / 2.],
    ])
    
    # 转换到栅格坐标
    pts = (pts - self.bx.cpu().numpy()) / (self.dx.cpu().numpy())
    pts[:, [0, 1]] = pts[:, [1, 0]]
    
    # 使用多边形填充获取车辆占用的所有栅格
    rr, cc = polygon(pts[:,1], pts[:,0])
    rc = np.concatenate([rr[:,None], cc[:,None]], axis=-1)
    
    # 对每个轨迹点，计算车辆占用区域
    trajs_ = trajs_ / self.dx.to(trajs.device)
    trajs_ = trajs_.cpu().numpy() + rc  # 添加车辆形状偏移
    
    # 检查碰撞
    collision = np.full(n_future, False)
    for t in range(n_future):
        rr = r[t]
        cc = c[t]
        I = np.logical_and(
            np.logical_and(rr >= 0, rr < self.bev_dimension[0]),
            np.logical_and(cc >= 0, cc < self.bev_dimension[1]),
        )
        # 如果车辆占用的任何栅格与物体重叠 → 碰撞
        collision[t] = np.any(segmentation[t, rr[I], cc[I]].cpu().numpy())
    
    return torch.from_numpy(collision).to(device=traj.device)
```

**调用位置**: 与Object Collision Rate相同

```python
obj_coll, obj_box_coll = self.planning_metric.evaluate_coll(
    pred_ego_fut_trajs[:, :cur_time].detach(),
    gt_ego_fut_trajs[:, :cur_time],
    occupancy
)
metric_dict['plan_obj_box_col_{}s'.format(i+1)] = np.nan_to_num(obj_box_coll.mean().item())
```

### 单位和含义

- **单位**: 比例（0-1之间）或百分比（0-100%）
- **含义**: 预测车辆边界框发生碰撞的帧数占比
- **越小越好**: 0表示无碰撞，1表示所有帧都碰撞

### 可视化示例

```
栅格图示例（BEV视角，向上为前方）：

Object Collision检测:          Bounding Box Collision检测:
        ↑                              ↑
        |                              |
    [   *   ]  ← 轨迹中心点         [#######]  ← 完整车辆边界框
    [       ]                        [#  *  #]
    [  OBJ  ]  ← 其他车辆            [#######]
                                     [  OBJ  ]

碰撞判定: 无碰撞 ✓               碰撞判定: 有碰撞 ✗
```

---

## 评估时间点

所有三个指标都在三个时间点计算：

| 时间 | 帧数 | 频率 | 说明 |
|------|------|------|------|
| **1秒** | 2帧 | 2 Hz | 短期预测 |
| **2秒** | 4帧 | 2 Hz | 中期预测 |
| **3秒** | 6帧 | 2 Hz | 长期预测 |

### 汇总计算

**代码位置**: `mmcv/datasets/suscape_orion_dataset.py` (Line 1322-1338)

```python
# 计算平均值
metric_dict['plan_L2_avg'] = (
    metric_dict['plan_L2_1s'] + 
    metric_dict['plan_L2_2s'] + 
    metric_dict['plan_L2_3s']
) / 3.0

metric_dict['plan_obj_col_avg'] = (
    metric_dict['plan_obj_col_1s'] + 
    metric_dict['plan_obj_col_2s'] + 
    metric_dict['plan_obj_col_3s']
) / 3.0

metric_dict['plan_obj_box_col_avg'] = (
    metric_dict['plan_obj_box_col_1s'] + 
    metric_dict['plan_obj_box_col_2s'] + 
    metric_dict['plan_obj_box_col_3s']
) / 3.0
```

---

## 评估结果示例

### 输出格式

**代码位置**: `mmcv/datasets/suscape_orion_dataset.py` (Line 1340-1360)

```python
print(f"\nTotal valid samples: {num_valid}\n")

print("L2 Trajectory Error (meters):")
print(f"  1s: {metric_dict['plan_L2_1s']:.4f}")
print(f"  2s: {metric_dict['plan_L2_2s']:.4f}")
print(f"  3s: {metric_dict['plan_L2_3s']:.4f}")
print(f"  Avg: {metric_dict['plan_L2_avg']:.4f}")

print("\nObject Collision Rate:")
print(f"  1s: {metric_dict['plan_obj_col_1s']:.4f}")
print(f"  2s: {metric_dict['plan_obj_col_2s']:.4f}")
print(f"  3s: {metric_dict['plan_obj_col_3s']:.4f}")
print(f"  Avg: {metric_dict['plan_obj_col_avg']:.4f}")

print("\nBounding Box Collision Rate:")
print(f"  1s: {metric_dict['plan_obj_box_col_1s']:.4f}")
print(f"  2s: {metric_dict['plan_obj_box_col_2s']:.4f}")
print(f"  3s: {metric_dict['plan_obj_box_col_3s']:.4f}")
print(f"  Avg: {metric_dict['plan_obj_box_col_avg']:.4f}")
```

### 实际输出示例

```
============================================================
Planning Metrics Evaluation (SUScape QA Dataset)
============================================================

Total valid samples: 24684

L2 Trajectory Error (meters):
  1s: 6.3168
  2s: 15.7243
  3s: 29.4661
  Avg: 17.1690

Object Collision Rate:
  1s: 0.0046
  2s: 0.0035
  3s: 0.0028
  Avg: 0.0036

Bounding Box Collision Rate:
  1s: 0.0001
  2s: 0.0004
  3s: 0.0014
  Avg: 0.0006

============================================================
```

### 结果解读

#### L2轨迹误差

- **1s: 6.32米** - 短期预测较准确
- **2s: 15.72米** - 中期预测误差增大
- **3s: 29.47米** - 长期预测误差最大
- **平均: 17.17米** - 整体预测精度

**解读**: 误差随预测时间增长，这是正常现象（误差累积）。

#### 物体碰撞率

- **1s: 0.46%** - 短期内极少碰撞
- **2s: 0.35%** - 碰撞率略降
- **3s: 0.28%** - 长期预测更保守
- **平均: 0.36%** - 整体安全性好

**解读**: 碰撞率随时间递减，可能是模型在长期预测时更保守。

#### 边界框碰撞率

- **1s: 0.01%** - 几乎无边界框碰撞
- **2s: 0.04%** - 极低碰撞率
- **3s: 0.14%** - 仍然很低
- **平均: 0.06%** - 整体非常安全

**解读**: 边界框碰撞率远低于物体碰撞率，说明即使中心点接近物体，边界框仍有安全距离。

---

## 性能基准

### 优秀性能标准

| 指标 | 1s | 2s | 3s |
|------|----|----|---- |
| **L2 Error** | <1m | <3m | <5m |
| **Object Collision** | <1% | <2% | <3% |
| **BBox Collision** | <0.5% | <1% | <1.5% |

### 当前结果评估

| 指标 | 1s | 2s | 3s | 评价 |
|------|----|----|----|----|
| **L2 Error** | 6.32m | 15.72m | 29.47m | ⚠️ 需改进 |
| **Object Collision** | 0.46% | 0.35% | 0.28% | ✅ 优秀 |
| **BBox Collision** | 0.01% | 0.04% | 0.14% | ✅ 优秀 |

**总体评价**:
- **安全性**: 优秀（碰撞率极低）
- **精度**: 需改进（L2误差较大，主要原因是域迁移）

---

## 改进建议

### 降低L2误差

1. **在SUScape数据上Fine-tune**
   - 当前使用nuScenes预训练模型直接评估
   - 预期改进: 60-80%

2. **优化损失函数**
   - 增加轨迹平滑性约束
   - 添加速度匹配损失

3. **数据增强**
   - 增加训练样本多样性

### 维持低碰撞率

- 当前碰撞率已经很好
- 保持现有的安全性机制
- 可以通过可视化验证

---

## 代码文件索引

### 核心计算

- **`mmcv/models/dense_heads/planning_head_plugin/metric_stp3.py`**
  - `PlanningMetric` 类
  - `compute_L2()` - L2误差计算
  - `evaluate_coll()` - 碰撞率计算
  - `evaluate_single_coll()` - 单样本碰撞检测
  - `get_birds_eye_view_label()` - BEV栅格构建

### 指标调用

- **`mmcv/models/detectors/orion.py`**
  - Line 1041-1042: 初始化PlanningMetric
  - Line 1043-1045: 构建占用栅格
  - Line 1050-1060: 计算并存储指标

### 结果汇总

- **`mmcv/datasets/suscape_orion_dataset.py`**
  - Line 1289-1366: `evaluate()` 方法
  - Line 1304-1319: 汇总所有样本的指标
  - Line 1322-1338: 计算平均值
  - Line 1340-1360: 打印结果

---

## 相关文档

- **L2误差计算与分析.md** - L2误差详细分析
- **测试分析与结果对比.md** - 测试结果对比
- **闭环与开环评估对比说明.md** - 评估方法对比

---

## 总结

### 三个指标的核心要点

| 指标 | 检测对象 | 计算方式 | 单位 | 越小越好 |
|------|---------|---------|------|---------|
| **L2 Error** | 轨迹点位置 | 欧氏距离平均 | 米 | ✓ |
| **Object Collision** | 轨迹中心点 | 栅格占用检测 | 比例 | ✓ |
| **BBox Collision** | 完整车辆边界框 | 多边形占用检测 | 比例 | ✓ |

### 评估流程

```
1. 加载数据 (SUScape QA dataset)
   ↓
2. 模型预测 (预测6帧未来轨迹)
   ↓
3. 构建BEV占用栅格 (投影所有物体)
   ↓
4. 计算指标 (1s/2s/3s三个时间点)
   ↓
5. 汇总结果 (平均所有样本)
   ↓
6. 打印报告
```

### 使用场景

- **开发阶段**: 使用开环评估快速迭代
- **性能验证**: 对比L2误差改进
- **安全测试**: 关注碰撞率指标
- **发布前**: 所有指标综合评估

---

**文档创建日期**: 2026-01-29  
**适用数据集**: SUScape  
**适用模型**: ORION
