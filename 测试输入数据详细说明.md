# 测试输入数据详细说明

## 概述

在SUScape数据集上测试ORION模型时，系统使用多种输入数据来生成预测轨迹。本文档详细说明所有输入数据的类型、来源、格式和处理流程。

**快速回答**: 模型使用**6个摄像头图像**、**CSV轨迹数据**、**QA数据集**（可选）以及**相机参数**来预测未来轨迹。

---

## 输入数据总览

### 核心输入（必需）

| 输入类型 | 数量/维度 | 来源 | 用途 |
|---------|----------|------|------|
| **多视角图像** | 6张 × 640×640 | 摄像头图像文件 | 视觉感知 |
| **CSV轨迹** | 每帧多个物体 | CSV文件 | 历史状态、GT轨迹 |
| **相机参数** | 6组 | 默认配置 | 图像投影 |
| **CAN总线** | 18维向量 | CSV派生 | ego车辆状态 |

### 额外输入（可选但推荐）

| 输入类型 | 数量/维度 | 来源 | 用途 |
|---------|----------|------|------|
| **QA数据集** | 9个任务 | ShareGPT JSON | LLM上下文、精确GT |

---

## 详细数据说明

### 1. 多视角摄像头图像 📷

#### 摄像头配置

模型使用**6个摄像头**覆盖360度视角：

```
CAM_FRONT_LEFT    CAM_FRONT    CAM_FRONT_RIGHT
        ↖            ↑            ↗
           \         |         /
            \        |        /
             [  自车位置  ]
            /        |        \
           /         |         \
        ↙            ↓            ↘
CAM_BACK_LEFT      CAM_BACK     CAM_BACK_RIGHT
```

#### 图像规格

| 属性 | 值 |
|------|-----|
| **原始分辨率** | 1600×900 |
| **处理后分辨率** | 640×640 |
| **格式** | RGB（3通道） |
| **归一化** | mean=[123.675, 116.28, 103.53], std=[58.395, 57.12, 57.375] |

#### 文件路径结构

**新结构**（推荐）：
```
data_root/
├── scene-000000/
│   ├── CAM_FRONT/
│   │   ├── 0.jpg
│   │   ├── 1.jpg
│   │   └── ...
│   ├── CAM_FRONT_LEFT/
│   ├── CAM_FRONT_RIGHT/
│   ├── CAM_BACK/
│   ├── CAM_BACK_LEFT/
│   └── CAM_BACK_RIGHT/
└── scene-000001/
    └── ...
```

**旧结构**：
```
data_root/raws/
├── scene-000000/
│   ├── 0.csv
│   ├── CAM_FRONT/
│   └── ...
```

#### 数据流

```
原始图像 (1600×900)
  ↓ Resize
调整大小 (0.37-0.45倍)
  ↓ Crop
裁剪 (320×640)
  ↓ Resize
最终大小 (640×640)
  ↓ Normalize
归一化
  ↓ Pad
填充到32的倍数
  ↓
送入模型 [B, 6, 3, 640, 640]
```

---

### 2. CSV轨迹数据 📊

#### CSV文件格式

**位置**: `csv_root/X.csv`（X为场景编号，如0.csv对应scene-000000）

**列定义**:
```
TIMESTAMP, TRACK_ID, OBJECT_TYPE, X, Y, V_X, V_Y, A_X, A_Y, YAW, DYAW, DDYAW, CITY_NAME
```

#### 字段说明

| 字段 | 类型 | 单位 | 说明 |
|------|------|------|------|
| **TIMESTAMP** | float | 秒 | 时间戳 |
| **TRACK_ID** | str | - | "ego"表示自车，其他为物体ID |
| **OBJECT_TYPE** | str | - | "Vehicle", "Pedestrian", "Bicycle" |
| **X, Y** | float | 米 | 世界坐标系中的位置 |
| **V_X, V_Y** | float | m/s | 速度（世界坐标系） |
| **A_X, A_Y** | float | m/s² | 加速度（世界坐标系） |
| **YAW** | float | 度 | 朝向角（转换为弧度） |
| **DYAW** | float | 度/s | 角速度 |
| **DDYAW** | float | 度/s² | 角加速度 |
| **CITY_NAME** | str | - | 城市名称 |

#### 数据提取

**Ego车辆数据**（每帧）:
```python
ego_data = frame_data[frame_data['TRACK_ID'] == 'ego']
ego_x, ego_y = ego_data['X'], ego_data['Y']
ego_vx, ego_vy = ego_data['V_X'], ego_data['V_Y']
ego_yaw = ego_data['YAW'] * π/180  # 转换为弧度
```

**其他物体数据**:
```python
objects_data = frame_data[frame_data['TRACK_ID'] != 'ego']
# 构建3D边界框 [x, y, z, w, l, h, yaw, vx, vy]
```

#### 用途

1. **历史轨迹**: 过去2帧（0.5秒）的ego位置
2. **当前状态**: 当前帧的ego位置、速度、加速度
3. **GT轨迹**: 未来6帧（3秒）的真实轨迹（用于评估）
4. **场景上下文**: 其他车辆、行人的位置和运动

---

### 3. QA数据集（ShareGPT格式）📝

#### 数据位置

```
qa_root/
├── dataset_info.json
├── suscape_NQA_q1.json   # VRU识别
├── suscape_NQA_q2.json   # 车辆识别
├── suscape_NQA_q3.json   # 交通标志
├── suscape_NQA_q4.json   # 交通信号灯
├── suscape_NQA_q5.json   # 障碍物
├── suscape_NQA_q6.json   # 场景描述
├── suscape_NQA_q7.json   # 轨迹预测 ⭐
├── suscape_NQA_q8.json   # 场景理解
└── suscape_NQA_q9.json   # 驾驶决策
```

#### Q7: 轨迹预测任务 ⭐

**最重要**: q7提供精确的GT轨迹，用于L2误差评估

**格式**:
```json
{
  "id": "scene-000000_frame-42",
  "conversations": [
    {
      "from": "human",
      "value": "预测未来3秒的轨迹"
    },
    {
      "from": "gpt",
      "value": "[(x1,y1), (x2,y2), ..., (x6,y6)]"
    }
  ]
}
```

**提取轨迹**:
```python
# 从GPT回复中提取坐标对
trajectory = [(x1,y1), (x2,y2), (x3,y3), (x4,y4), (x5,y5), (x6,y6)]
# 6个点，每个点间隔0.5秒
```

#### Q1-Q6, Q8-Q9: 场景理解

**用途**: 为LLM提供上下文信息

**示例**:
- Q1: "前方有行人吗？" → "是，左前方有2个行人"
- Q2: "周围有哪些车辆？" → "左侧有1辆车，后方有2辆车"
- Q6: "描述当前场景" → "城市道路，直行，有行人和车辆"

#### 数据流

```
QA JSON文件
  ↓ 解析
提取对话对
  ↓ Tokenize
转换为token IDs
  ↓ 构建
input_ids + vlm_labels
  ↓
送入LLM模块
```

---

### 4. 相机参数 🎥

#### 内参矩阵（Intrinsic）

**默认值**（可配置）:
```python
intrinsic = [
    [fx,  0, cx],    # fx=640, cx=640
    [ 0, fy, cy],    # fy=640, cy=360
    [ 0,  0,  1]
]
```

**用途**: 将3D点投影到2D图像平面

#### 外参矩阵（Extrinsic）

**cam2ego转换**: 将相机坐标系转换到ego车辆坐标系

**默认配置**:
```python
CAM_FRONT:       [1.7, 0.0, 1.5]  # 车头前方1.7米
CAM_FRONT_LEFT:  [1.5, -0.5, 1.5] # 左前方
CAM_FRONT_RIGHT: [1.5, 0.5, 1.5]  # 右前方
CAM_BACK:        [-1.0, 0.0, 1.5] # 车尾后方1米
CAM_BACK_LEFT:   [-1.0, -0.5, 1.5]
CAM_BACK_RIGHT:  [-1.0, 0.5, 1.5]
```

#### lidar2img转换

**计算流程**:
```python
# 1. ego到世界坐标
world2lidar = 构建从ego坐标系的变换矩阵
lidar2global = world2lidar的逆

# 2. lidar到相机
lidar2cam = cam2ego的逆 @ lidar2ego

# 3. 相机到图像
lidar2img = intrinsic @ lidar2cam
```

**用途**: 
- 将3D物体投影到图像
- 图像特征的3D位置编码

---

### 5. CAN总线数据 🚗

#### 18维向量

**代码位置**: `suscape_orion_dataset.py::get_data_info()`

```python
can_bus = np.zeros(18)
can_bus[:3] = ego_translation      # [0:3]   位置 (x, y, z)
can_bus[3:7] = rotation_quaternion # [3:7]   四元数旋转
can_bus[7:10] = ego_vel            # [7:10]  速度 (vx, vy, vz)
can_bus[10:13] = ego_accel         # [10:13] 加速度 (ax, ay, az)
can_bus[13:16] = ego_rotation_rate # [13:16] 角速度 (0, 0, dyaw)
can_bus[16] = yaw                  # [16]    yaw角（弧度）
can_bus[17] = yaw_in_degree        # [17]    yaw角（度）
```

#### 详细说明

| 索引 | 维度 | 内容 | 来源 | 单位 |
|------|------|------|------|------|
| 0-2 | 3 | ego位置 (x, y, z) | CSV | 米 |
| 3-6 | 4 | 旋转四元数 | CSV (yaw派生) | - |
| 7-9 | 3 | 速度 (vx, vy, vz) | CSV | m/s |
| 10-12 | 3 | 加速度 (ax, ay, az) | CSV | m/s² |
| 13-15 | 3 | 角速度 (0, 0, dyaw) | CSV | rad/s |
| 16 | 1 | yaw角 | CSV | 弧度 |
| 17 | 1 | yaw角 | CSV | 度 |

#### 用途

- 提供ego车辆的完整状态信息
- 用于轨迹预测模块
- 用于特征增强

---

### 6. 历史轨迹 📍

#### ego_his_trajs

**维度**: `[past_frames, 2]` = `[2, 2]`

**内容**: 过去2帧（1秒）的ego车辆位置

**代码**:
```python
# 从CSV获取过去2帧的数据
past_frames = 2
ego_his_trajs = []
for t in range(-past_frames, 0):
    past_ego = 从CSV获取t时刻的ego数据
    ego_his_trajs.append([past_ego.X, past_ego.Y])

ego_his_trajs = np.array(ego_his_trajs)  # shape: [2, 2]
```

**用途**: 
- 提供运动历史
- 帮助模型理解当前运动趋势

---

## 完整数据流

### 数据加载流程

```
1. 初始化数据集
   ├── 读取PKL annotation文件
   ├── 或生成annotations（从CSV+图像）
   └── 设置数据pipeline

2. 对每个样本 (idx)
   ├── get_data_info(idx)
   │   ├── 加载CSV数据 → ego状态、物体
   │   ├── 构建相机路径
   │   ├── 计算变换矩阵
   │   └── 生成CAN总线向量
   │
   ├── Data Pipeline
   │   ├── LoadMultiViewImageFromFiles → 加载6张图像
   │   ├── LoadAnnotations3D → 加载3D标注
   │   ├── ResizeCropFlipRotImage → 图像增强
   │   ├── NormalizeMultiviewImage → 归一化
   │   ├── LoadAnnoatationCriticalVQA → 加载QA数据
   │   └── PETRFormatBundle3D → 格式化
   │
   └── Collate → 批处理

3. 输出batch
   ├── img: [B, 6, 3, 640, 640]
   ├── img_metas: 元数据列表
   ├── gt_bboxes_3d: 3D边界框
   ├── can_bus: [B, 18]
   ├── ego_his_trajs: [B, 2, 2]
   ├── ego_fut_trajs: [B, 6, 2] (GT)
   ├── input_ids: QA输入
   └── 其他...
```

### 模型前向流程

```
输入Batch
  ↓
1. 图像编码
   img [B,6,3,640,640] → img_feats [B,6,C,H,W]
   ↓
2. BEV特征提取
   img_feats + lidar2img → bev_feats [B,C,H,W]
   ↓
3. 时序融合
   bev_feats + memory → temporal_feats
   ↓
4. LLM上下文（如果有QA）
   input_ids → llm_context
   ↓
5. 轨迹预测
   temporal_feats + can_bus + llm_context
   ↓
   ego_fut_preds [B, 6, 2]  ← 预测轨迹！
```

---

## 配置示例

### suscape_eval.py核心配置

```python
# 数据路径
data_root = "/path/to/suscape_scenes"     # 包含scene-XXXXXX文件夹
csv_root = "/path/to/csv_files"           # 包含X.csv文件
qa_root = "/path/to/sharegpt_dataset"     # 包含qX.json文件

# QA任务
qa_tasks = ["q1", "q2", "q3", "q4", "q5", "q6", "q7", "q8", "q9"]
# q1-q6, q8-q9: LLM上下文
# q7: 精确GT轨迹

# 时间配置
past_frames = 2   # 历史2帧 (1秒)
future_frames = 6 # 预测6帧 (3秒)

# 输入模态
input_modality = dict(
    use_lidar=False,    # 不使用激光雷达
    use_camera=True,    # 使用摄像头 ✓
    use_radar=False,    # 不使用雷达
    use_map=False,      # 不使用地图（SUScape无地图）
    use_external=True)  # 使用外部信息（CAN总线、QA等）
```

---

## 输入数据总结表

### 必需输入

| 数据 | 形状 | 来源 | 用途 |
|------|------|------|------|
| **img** | [B,6,3,640,640] | 图像文件 | 视觉感知 |
| **lidar2img** | [B,6,4,4] | 计算 | 坐标变换 |
| **can_bus** | [B,18] | CSV派生 | ego状态 |
| **ego_his_trajs** | [B,2,2] | CSV | 历史轨迹 |
| **gt_bboxes_3d** | [B,N,9] | CSV | 场景物体 |
| **img_metas** | List[Dict] | 多源 | 元数据 |

### 可选输入（但推荐）

| 数据 | 形状 | 来源 | 用途 |
|------|------|------|------|
| **input_ids** | [B,L] | QA JSON | LLM输入 |
| **vlm_labels** | [B,L] | QA JSON | LLM标签 |
| **ego_fut_trajs** | [B,6,2] | QA q7 | 精确GT |

---

## 常见问题

### Q1: 为什么需要6个摄像头？

**A**: 360度视角覆盖，提供完整的环境感知：
- 前方3个：检测前方车辆、行人、障碍物
- 后方3个：检测后方和侧方车辆，完整感知

### Q2: CSV和QA数据有什么区别？

**A**: 
- **CSV**: 原始轨迹数据，精度一般，所有场景都有
- **QA q7**: 经过人工标注的高精度轨迹，推荐用于评估
- **QA q1-q6,q8-q9**: 场景理解问答，为LLM提供上下文

### Q3: 如果没有QA数据怎么办？

**A**: 可以运行，但：
- L2误差评估使用CSV数据（精度较低）
- LLM没有额外上下文（性能可能下降）
- 建议获取QA数据以获得最佳效果

### Q4: 输入图像为什么要Resize到640×640？

**A**: 
- 模型训练时使用640×640
- 保持一致的输入尺寸
- 平衡性能和计算效率

### Q5: CAN总线数据是必需的吗？

**A**: 是的，CAN总线提供ego车辆的完整状态信息，是轨迹预测的关键输入。

---

## 数据准备检查清单

### 测试前确认

- [ ] 图像文件存在且可读 (6个摄像头 × 每帧)
- [ ] CSV文件格式正确（13列）
- [ ] CSV包含ego和其他物体数据
- [ ] QA数据集（可选但推荐）
- [ ] data_root和csv_root路径正确
- [ ] 图像文件名与帧索引对应

### 快速验证

```bash
# 1. 检查场景数量
ls -d ${data_root}/scene-* | wc -l

# 2. 检查CSV文件
ls ${csv_root}/*.csv | wc -l

# 3. 检查QA文件
ls ${qa_root}/suscape_NQA_q*.json | wc -l

# 4. 检查图像
ls ${data_root}/scene-000000/CAM_FRONT/*.jpg | wc -l
```

---

## 相关文件

### 代码文件

| 文件 | 功能 |
|------|------|
| `mmcv/datasets/suscape_orion_dataset.py` | 数据集实现 |
| `adzoo/orion/configs/suscape_eval.py` | 评估配置 |
| `mmcv/models/detectors/orion.py` | 模型forward |
| `mmcv/datasets/pipelines/` | 数据处理pipeline |

### 文档

| 文档 | 内容 |
|------|------|
| **测试输入数据详细说明.md** | 本文档 |
| 评估指标详细说明.md | 评估指标 |
| SUScape_Finetune快速上手.md | Fine-tune指南 |

---

## 总结

测试时使用的输入数据：

1. **6个摄像头图像** (640×640) - 视觉感知
2. **CSV轨迹数据** - ego状态、历史轨迹、场景物体
3. **QA数据集** - LLM上下文、精确GT轨迹
4. **相机参数** - 内参、外参、坐标变换
5. **CAN总线** - 18维ego状态向量
6. **历史轨迹** - 过去2帧位置

这些数据综合提供了完整的场景感知和状态信息，使模型能够预测未来6帧（3秒）的轨迹。

**关键**: 所有输入都在ego车辆坐标系中处理，保证了一致性和准确性。
