# 闭环与开环评估对比说明

## 快速回答

**问题**: 闭环测评是要连接CARLA吗？

**回答**: 
- **闭环评估（Closed-Loop Evaluation）**: ✅ **需要连接CARLA仿真器**
- **开环评估（Open-Loop Evaluation）**: ❌ **不需要CARLA，使用预录数据**

---

## 目录

1. [评估方法对比](#评估方法对比)
2. [开环评估（当前实现）](#开环评估当前实现)
3. [闭环评估（需要CARLA）](#闭环评估需要carla)
4. [何时使用哪种方法](#何时使用哪种方法)
5. [CARLA闭环评估配置](#carla闭环评估配置)
6. [优缺点对比](#优缺点对比)

---

## 评估方法对比

### 开环评估 (Open-Loop Evaluation)

```
预录数据 → 模型预测 → 与真实轨迹对比 → 计算指标
```

**特点**:
- ✅ 使用预先录制的数据集
- ✅ 不需要仿真器或实时交互
- ✅ 快速、可重复
- ❌ 无法评估动态交互能力
- ❌ 无法检测累积误差

### 闭环评估 (Closed-Loop Evaluation)

```
CARLA仿真 → 模型预测 → 控制车辆 → 观察结果 → 评估性能
```

**特点**:
- ✅ 实时仿真环境
- ✅ 可评估动态交互和累积误差
- ✅ 更接近真实驾驶场景
- ❌ 需要CARLA仿真器
- ❌ 运行速度较慢
- ❌ 配置复杂

---

## 开环评估（当前实现）

### 当前SUScape实现

**位置**: `mmcv/datasets/suscape_orion_dataset.py`

**工作原理**:

1. **数据加载**:
   ```python
   # 从CSV文件读取轨迹
   data/suscape_scenes/raws/scene-000000/0.csv
   ```

2. **模型预测**:
   ```python
   # 模型输出未来轨迹
   predicted_trajectory = model(images, past_trajectory)
   ```

3. **指标计算**:
   ```python
   # 与真实轨迹对比
   L2_error = compute_L2(predicted_trajectory, ground_truth_trajectory)
   collision_rate = check_collision(predicted_trajectory, obstacles)
   ```

### 使用方法

```bash
# 运行开环评估（不需要CARLA）
python adzoo/orion/test.py \
    adzoo/orion/configs/suscape_eval.py \
    ckpts/Orion.pth \
    --eval bbox
```

### 评估指标

```
L2 Trajectory Error (meters):
  1s: 6.3168
  2s: 15.7243
  3s: 29.4661

Object Collision Rate:
  1s: 0.0046
  2s: 0.0035
  3s: 0.0028

Bounding Box Collision Rate:
  1s: 0.0001
  2s: 0.0004
  3s: 0.0014
```

---

## 闭环评估（需要CARLA）

### CARLA集成代码

**位置**: `team_code/orion_b2d_agent.py`

**工作原理**:

1. **连接CARLA**:
   ```python
   import carla
   
   # 连接到CARLA服务器
   client = carla.Client('localhost', 2000)
   world = client.get_world()
   ```

2. **实时交互**:
   ```python
   # 获取传感器数据
   images = get_camera_images()
   
   # 模型预测
   trajectory = model(images)
   
   # 控制车辆
   control = carla.VehicleControl()
   control.steer = pid_controller(trajectory)
   vehicle.apply_control(control)
   ```

3. **评估结果**:
   ```python
   # 评估任务完成情况
   route_completion = calculate_route_completion()
   infractions = count_traffic_violations()
   ```

### CARLA版本要求

```python
# team_code/planner.py 第47-50行
# self.mean = np.array([49.0, 8.0])  # for carla 9.9
# self.scale = np.array([111324.60662786, 73032.1570362])  # for carla 9.9
self.mean = np.array([0.0, 0.0])  # for carla 9.10
self.scale = np.array([111324.60662786, 111319.490945])  # for carla 9.10
```

**推荐**: CARLA 0.9.10+

---

## 何时使用哪种方法

### 使用开环评估 ✅

**适用场景**:
1. ✅ **快速原型验证** - 快速测试模型性能
2. ✅ **大规模评估** - 需要评估大量场景
3. ✅ **基准对比** - 与其他方法对比L2误差等指标
4. ✅ **无CARLA环境** - 没有CARLA或GPU资源有限
5. ✅ **可重复实验** - 需要完全可重复的结果

**命令**:
```bash
# SUScape开环评估
./adzoo/orion/orion_dist_eval.sh \
    adzoo/orion/configs/suscape_eval.py \
    ckpts/Orion.pth \
    1
```

### 使用闭环评估 ✅

**适用场景**:
1. ✅ **真实场景测试** - 评估动态交互能力
2. ✅ **累积误差分析** - 检测长期预测的累积效应
3. ✅ **安全性验证** - 评估碰撞避免和规则遵守
4. ✅ **端到端验证** - 完整的感知-规划-控制链路测试
5. ✅ **Benchmark提交** - Bench2Drive等竞赛要求

**前提条件**:
```bash
# 1. 安装CARLA
wget http://carla-releases.s3.amazonaws.com/Linux/CARLA_0.9.10.1.tar.gz
tar -xzf CARLA_0.9.10.1.tar.gz

# 2. 启动CARLA服务器
cd CARLA_0.9.10.1
./CarlaUE4.sh -quality-level=Low -RenderOffScreen

# 3. 运行闭环评估
python team_code/run_agent.py \
    --agent orion_b2d_agent \
    --config configs/carla_config.yaml
```

---

## CARLA闭环评估配置

### 1. 安装CARLA

#### 方法1: 预编译版本（推荐）

```bash
# 下载CARLA 0.9.10.1
wget http://carla-releases.s3.amazonaws.com/Linux/CARLA_0.9.10.1.tar.gz

# 解压
mkdir CARLA_0.9.10.1
tar -xzf CARLA_0.9.10.1.tar.gz -C CARLA_0.9.10.1

# 安装Python API
cd CARLA_0.9.10.1/PythonAPI/carla/dist
pip install carla-0.9.10-py3.7-linux-x86_64.egg
```

#### 方法2: Docker（便捷）

```bash
# 拉取CARLA Docker镜像
docker pull carlasim/carla:0.9.10

# 运行CARLA服务器
docker run -p 2000-2002:2000-2002 \
    --runtime=nvidia \
    --gpus all \
    carlasim/carla:0.9.10 \
    /bin/bash CarlaUE4.sh -quality-level=Low -RenderOffScreen
```

### 2. 配置ORION Agent

**文件**: `team_code/orion_b2d_agent.py`

关键配置：

```python
# CARLA连接
CARLA_HOST = 'localhost'
CARLA_PORT = 2000

# 模型路径
MODEL_CHECKPOINT = 'ckpts/Orion.pth'
MODEL_CONFIG = 'adzoo/orion/configs/orion_stage3.py'

# PID控制器参数
PID_PARAMS = {
    'K_P': 1.0,
    'K_I': 0.05,
    'K_D': 0.1
}
```

### 3. 运行闭环评估

```bash
# 启动CARLA服务器（终端1）
cd CARLA_0.9.10.1
./CarlaUE4.sh -quality-level=Low -RenderOffScreen

# 运行评估（终端2）
cd /path/to/Orion_suscape
python team_code/run_agent.py \
    --agent orion_b2d_agent \
    --routes data/routes/route_01.xml \
    --checkpoint ckpts/Orion.pth
```

### 4. 闭环评估指标

```
Route Completion (%):     95.2%
Infraction Score:         0.85
Average Speed (km/h):     32.4
Collision Count:          2
Red Light Violations:     0
Lane Invasion Count:      5
```

---

## 优缺点对比

### 开环评估

#### ✅ 优点

| 优点 | 说明 |
|------|------|
| **速度快** | 无需实时仿真，批量处理 |
| **可重复** | 固定数据集，结果一致 |
| **易部署** | 只需模型和数据，无需CARLA |
| **大规模** | 可评估数万场景 |
| **成本低** | CPU即可运行，无需高端GPU |

#### ❌ 缺点

| 缺点 | 说明 |
|------|------|
| **无交互** | 无法评估动态交互 |
| **累积误差** | 无法检测长期累积效应 |
| **不够真实** | 预录数据无法覆盖所有情况 |
| **无反馈** | 预测错误不会影响后续状态 |

### 闭环评估

#### ✅ 优点

| 优点 | 说明 |
|------|------|
| **真实交互** | 实时与环境交互 |
| **累积效应** | 可检测长期预测误差 |
| **安全测试** | 评估碰撞避免能力 |
| **完整链路** | 测试感知-规划-控制全流程 |

#### ❌ 缺点

| 缺点 | 说明 |
|------|------|
| **需要CARLA** | 安装和配置复杂 |
| **速度慢** | 实时仿真耗时 |
| **资源需求高** | 需要高端GPU |
| **可重复性差** | 随机性导致结果波动 |

---

## 推荐工作流程

### 开发阶段

```
1. 开环评估（SUScape数据）
   ↓ 快速迭代
2. 模型优化
   ↓ 改进参数
3. 开环评估验证
   ↓ 达到目标后
4. 闭环评估（CARLA）
   ↓ 真实场景测试
5. 最终部署
```

### 评估策略

```python
# 阶段1: 开环快速评估（每天）
python adzoo/orion/test.py adzoo/orion/configs/suscape_eval.py ckpts/latest.pth

# 阶段2: 闭环验证（每周）
python team_code/run_agent.py --agent orion_b2d_agent --routes data/routes/

# 阶段3: 完整闭环评估（发布前）
./scripts/run_full_evaluation.sh
```

---

## 总结

### 当前SUScape实现

✅ **开环评估** - 不需要CARLA
- 使用CSV数据和图像
- 计算L2误差和碰撞率
- 快速、可重复、易部署

### 闭环评估需求

⚠️ **需要CARLA** - 如果要进行闭环评估
- 安装CARLA 0.9.10+
- 使用`team_code/orion_b2d_agent.py`
- 实时仿真和交互
- 评估动态性能

### 选择建议

| 目标 | 推荐方法 |
|------|---------|
| 快速验证模型 | 开环评估 |
| 大规模基准测试 | 开环评估 |
| 发布前验证 | 开环 + 闭环 |
| Bench2Drive竞赛 | 闭环评估 |
| 学术论文对比 | 开环评估 |
| 安全性测试 | 闭环评估 |

---

## 参考资料

### 开环评估
- 文档: `docs/SUSCAPE_EVALUATION.md`
- 数据集: `mmcv/datasets/suscape_orion_dataset.py`
- 配置: `adzoo/orion/configs/suscape_eval.py`

### 闭环评估  
- Agent: `team_code/orion_b2d_agent.py`
- Planner: `team_code/planner.py`
- PID控制: `team_code/pid_controller.py`

### CARLA资源
- 官网: https://carla.org/
- 文档: https://carla.readthedocs.io/
- GitHub: https://github.com/carla-simulator/carla

### Benchmark
- Bench2Drive: https://github.com/Thinklab-SJTU/Bench2Drive
- nuScenes: https://www.nuscenes.org/

---

## FAQ

### Q1: 我只想快速测试模型，用哪种方法？

**A**: 使用**开环评估**，不需要CARLA：
```bash
python adzoo/orion/test.py adzoo/orion/configs/suscape_eval.py ckpts/Orion.pth
```

### Q2: 我想参加Bench2Drive竞赛，用哪种方法？

**A**: 需要**闭环评估**，必须安装CARLA并使用`orion_b2d_agent.py`。

### Q3: 开环评估的结果可靠吗？

**A**: 
- ✅ 对于**L2误差**等指标：可靠
- ⚠️ 对于**动态交互**能力：不够全面
- 建议：开环快速迭代 + 闭环最终验证

### Q4: 如何从开环切换到闭环？

**A**: 
1. 安装CARLA 0.9.10+
2. 配置`team_code/orion_b2d_agent.py`
3. 运行agent脚本

### Q5: CARLA太慢怎么办？

**A**: 
- 使用`-quality-level=Low`降低渲染质量
- 使用`-RenderOffScreen`无头模式
- 或坚持使用开环评估进行日常开发

---

**最后更新**: 2026-01-29
