# 训练故障排除指南

## 问题诊断步骤

如果您在运行训练时遇到错误，请按照以下步骤进行诊断：

### 1. 获取完整错误信息

**重要**：请提供完整的错误堆栈，而不仅仅是第一行。

例如，如果您看到：
```
Traceback (most recent call last):
  File "adzoo/orion/train.py", line 242, in <module>
```

请继续复制后续的所有错误信息，直到看到实际的错误类型和消息。

### 2. 检查所有已知修复是否已应用

确保您的代码包含所有6个已修复的问题：

| 问题 | 状态 | 相关文档 |
|------|------|---------|
| 1. `--load-from` 参数 | ✅ | 训练参数修复说明.md |
| 2. Tokenizer验证错误 | ✅ | Tokenizer加载错误最终修复.md |
| 3. Tokenizer路径不匹配 | ✅ | Tokenizer路径配置问题解决方案.md |
| 4. PKL文件格式 | ✅ | PKL文件格式错误修复说明.md |
| 5. DataLoader配置 | ✅ | DataLoader配置错误修复.md |
| 6. VQA数据格式 | ✅ | VQA数据格式错误修复.md |

**验证命令**：
```bash
git pull origin copilot/modify-program-for-evaluation
```

### 3. 验证环境配置

#### 检查Python环境
```bash
python --version  # 应该是Python 3.8+
```

#### 检查必需的包
```bash
python -c "import torch; print('PyTorch:', torch.__version__)"
python -c "import cv2; print('OpenCV: OK')"
python -c "import mmcv; print('MMCV: OK')"
```

#### 检查CUDA（如果使用GPU）
```bash
python -c "import torch; print('CUDA available:', torch.cuda.is_available())"
```

### 4. 验证数据和模型文件

#### 检查符号链接
```bash
ls -la ./ckpts/pretrain_qformer/
```

应该能看到tokenizer文件。

#### 检查数据文件
```bash
ls -la data/suscape_infos/suscape_infos_test.pkl
```

#### 检查检查点文件
```bash
ls -lh /path/to/your/checkpoint.pth
```

### 5. 常见错误及解决方案

#### 错误 1: ModuleNotFoundError
```
ModuleNotFoundError: No module named 'xxx'
```

**解决**：安装缺失的包
```bash
pip install -r requirements.txt
```

#### 错误 2: FileNotFoundError
```
FileNotFoundError: [Errno 2] No such file or directory
```

**解决**：
1. 检查文件路径是否正确
2. 检查符号链接是否创建
3. 检查工作目录是否正确

#### 错误 3: AttributeError in DC/DataContainer
```
AttributeError: 'str' object has no attribute 'get'
```

**解决**：参见 PKL文件格式错误修复说明.md

#### 错误 4: HuggingFace Validation Error
```
HFValidationError: Repo id must be in the form...
```

**解决**：参见 Tokenizer加载错误最终修复.md

#### 错误 5: TypeError with pad_sequence
```
TypeError: expected Tensor as element 0 in argument 0, but got list
```

**解决**：参见 VQA数据格式错误修复.md

### 6. 如何获取帮助

如果您遇到新的错误，请提供以下信息：

1. **完整的错误堆栈**（从`Traceback`开始到错误消息结束）
2. **运行的命令**
3. **环境信息**：
   ```bash
   python --version
   pip list | grep -E 'torch|mmcv|opencv'
   ```
4. **配置文件**：您使用的配置文件名称
5. **数据集信息**：数据文件的位置和大小

### 7. 完整的训练命令模板

```bash
# 确保在正确的目录
cd /path/to/Orion_suscape

# 创建符号链接（如果需要）
ln -s /path/to/real/ckpts ./ckpts

# 运行训练
python adzoo/orion/train.py \
    adzoo/orion/configs/suscape_finetune.py \
    --load-from /path/to/checkpoint.pth \
    --work-dir work_dirs/orion_suscape_finetune \
    --gpus 1  # 或使用 --gpu-ids 0 1 2 3
```

### 8. 日志和调试

#### 启用详细日志
在配置文件中设置：
```python
log_level = 'DEBUG'
```

#### 检查训练日志
```bash
tail -f work_dirs/orion_suscape_finetune/logs/*.log
```

#### 使用pdb调试
在可疑代码处添加：
```python
import pdb; pdb.set_trace()
```

### 9. 快速检查清单

在报告问题前，请确认：

- [ ] 代码是最新的（`git pull`）
- [ ] 所有依赖已安装（`pip install -r requirements.txt`）
- [ ] 符号链接已创建（如果需要）
- [ ] 数据文件存在且可访问
- [ ] 检查点文件存在且可访问
- [ ] 已提供**完整的错误消息**

### 10. 相关文档索引

- **入门**: README_微调.md
- **完整指南**: 微调完整指南.md
- **所有修复**: 所有训练问题完整解决方案总结.md
- **参数说明**: TRAINING_ARGUMENTS.md
- **检查点使用**: 使用自定义检查点指南.md

---

**提示**：大多数问题都是由于：
1. 缺少必要的文件或符号链接
2. 配置路径不正确
3. 环境依赖缺失
4. 使用了旧版本的代码

请先检查这些常见原因！
