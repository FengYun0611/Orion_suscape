# L2误差计算与分析

## 概述

本文档详细分析SUScape评估中L2轨迹误差的计算方法，解释为什么误差会比较大，以及如何理解和改进这些指标。

---

## 测试结果回顾

您的测试结果显示：

```
L2 Trajectory Error (meters):
  1s: 6.3168
  2s: 15.7243
  3s: 29.4661
  Avg: 17.1690
```

**单位**: 米（meters）

---

## L2误差计算方法

### 1. 源代码位置

**文件**: `mmcv/models/dense_heads/planning_head_plugin/metric_stp3.py`  
**函数**: `compute_L2()`

### 2. 计算公式

```python
def compute_L2(self, trajs, gt_trajs):
    '''
    trajs: torch.Tensor (n_future, 2)  # 预测轨迹
    gt_trajs: torch.Tensor (n_future, 2)  # 真实轨迹
    '''
    pred_len = trajs.shape[0]
    ade = float(
        sum(
            torch.sqrt(
                (trajs[i, 0] - gt_trajs[i, 0]) ** 2
                + (trajs[i, 1] - gt_trajs[i, 1]) ** 2
            )
            for i in range(pred_len)
        )
        / pred_len
    )
    return ade
```

### 3. 计算步骤详解

**L2误差使用的是ADE（Average Displacement Error）**:

1. **对于每个时间步**:
   ```
   distance_i = sqrt((pred_x_i - gt_x_i)² + (pred_y_i - gt_y_i)²)
   ```
   计算预测位置与真实位置之间的欧氏距离

2. **求平均**:
   ```
   ADE = (distance_0 + distance_1 + ... + distance_n) / n
   ```
   对所有时间步的距离求平均

3. **分时段计算**:
   - **1s预测**: 前2帧（0.5s采样率 × 2 = 1s）
   - **2s预测**: 前4帧（0.5s采样率 × 4 = 2s）
   - **3s预测**: 前6帧（0.5s采样率 × 6 = 3s）

### 4. 调用流程

```
orion.py::compute_planner_metric_stp3()
    ↓
    for i in range(3):  # 1s, 2s, 3s
        cur_time = (i+1)*2  # 2, 4, 6帧
        ↓
        traj_L2 = self.planning_metric.compute_L2(
            pred_ego_fut_trajs[0, :cur_time],  # 预测轨迹前cur_time帧
            gt_ego_fut_trajs[0, :cur_time]      # 真实轨迹前cur_time帧
        )
        ↓
        metric_dict['plan_L2_{}s'.format(i+1)] = traj_L2
```

---

## 数据说明

### 1. 轨迹表示

**坐标系**: 自车Lidar坐标系
```
    ^ y (侧向)
    |
    | 
    0-------> x (前向)
```

**轨迹格式**:
- `(n_future, 2)`: n_future个时间步，每步2维坐标(x, y)
- 单位: 米（meters）
- 参考点: 当前自车位置

### 2. 累积轨迹

**关键代码**:
```python
# 在orion.py中
ego_fut_pred = ego_fut_preds.cumsum(dim=-2)  # 预测的位移累积成轨迹
ego_fut_trajs = data['ego_fut_trajs'][0, 0].cumsum(dim=-2)  # GT位移累积
```

**说明**:
- 模型输出的是**相对位移**（displacement）
- 通过`cumsum`累积成**绝对位置**（position）
- 这样第i帧的坐标 = sum(第0到i帧的位移)

---

## 为什么L2误差会这么大？

### 1. 误差增长特性

**观察到的趋势**:
```
1s: 6.32m   ← 基线误差
2s: 15.72m  ← 2.5倍增长
3s: 29.47m  ← 4.7倍增长
```

**原因**:

#### (1) 误差累积效应
```
轨迹 = 位移的累积和 (cumsum)

如果每步位移有误差δ:
- 第1步误差: δ₁
- 第2步误差: δ₁ + δ₂
- 第3步误差: δ₁ + δ₂ + δ₃
...
```

**累积误差导致长期预测误差呈指数增长！**

#### (2) 预测难度增加
- **1秒预测**: 场景变化小，较容易预测
- **2秒预测**: 需要理解车辆意图
- **3秒预测**: 需要预测其他车辆行为、交通规则等

#### (3) 不确定性增长
```
时间越长 → 可能性空间越大 → 预测难度越高
```

### 2. 数值分析

#### 平均每步误差
```
1s (2帧): 6.32m ÷ 2 = 3.16 m/步
2s (4帧): 15.72m ÷ 4 = 3.93 m/步
3s (6帧): 29.47m ÷ 6 = 4.91 m/步
```

**发现**: 
- 每步的平均误差从3.16m增长到4.91m
- 说明**后期预测质量下降**

#### 与最后一帧误差对比

如果这是FDE（Final Displacement Error），3s的误差可能接近30米。
但由于这是ADE（所有帧平均），实际**最后一帧的误差可能更大**（可能40-50米）！

### 3. 与标准性能对比

**nuScenes常见指标**:
- 优秀模型1s ADE: ~0.5-1.0m
- 优秀模型3s ADE: ~2.0-4.0m

**您的结果**:
- 1s: 6.32m（偏高约6倍）
- 3s: 29.47m（偏高约7-10倍）

**可能原因**:

#### (1) 域迁移问题 ⭐⭐⭐
```
训练数据: nuScenes (城市道路)
测试数据: SUScape (可能是不同场景)
```
- 数据分布不同
- 驾驶模式不同
- 传感器配置不同

#### (2) 模型未针对SUScape微调 ⭐⭐⭐
```
当前: 使用nuScenes预训练权重直接评估
应该: 在SUScape数据上fine-tune
```

#### (3) 类别映射问题 ⭐⭐
```python
# 在suscape_eval.py中
cat_mapping = {
    'Vehicle': 'car',
    'Pedestrian': 'pedestrian',
    'Bicycle': 'bicycle'
}
```
- SUScape类别 → nuScenes类别的映射可能不准确
- 导致模型理解场景时出现偏差

#### (4) 数据预处理差异 ⭐⭐
- SUScape和nuScenes的坐标系可能有差异
- 数据标注方式可能不同
- 时间采样率可能不同

#### (5) 模型架构限制 ⭐
- 3秒预测本身就很困难
- 需要长期记忆和推理能力
- 当前模型可能缺少这种能力

---

## 深入分析

### 1. 轨迹可视化建议

建议添加可视化代码来检查：

```python
import matplotlib.pyplot as plt

def visualize_trajectory(pred_traj, gt_traj, step):
    plt.figure(figsize=(10, 10))
    plt.plot(gt_traj[:, 0], gt_traj[:, 1], 'g-o', label='Ground Truth', linewidth=2)
    plt.plot(pred_traj[:, 0], pred_traj[:, 1], 'r-x', label='Prediction', linewidth=2)
    plt.plot([0], [0], 'b*', markersize=15, label='Current Position')
    plt.legend()
    plt.grid(True)
    plt.axis('equal')
    plt.xlabel('X (meters)')
    plt.ylabel('Y (meters)')
    plt.title(f'Trajectory Comparison - Step {step}')
    plt.savefig(f'traj_vis_step_{step}.png')
    plt.close()
```

### 2. 误差分解分析

建议计算：

```python
# 横向误差（Lateral Error）
lateral_errors = torch.abs(pred_traj[:, 1] - gt_traj[:, 1])

# 纵向误差（Longitudinal Error）  
longitudinal_errors = torch.abs(pred_traj[:, 0] - gt_traj[:, 0])

# 分析哪个方向误差更大
print(f"Lateral ADE: {lateral_errors.mean():.2f}m")
print(f"Longitudinal ADE: {longitudinal_errors.mean():.2f}m")
```

这可以帮助判断：
- 转向预测是否准确（横向）
- 速度预测是否准确（纵向）

### 3. 逐帧误差分析

```python
# 计算每一帧的误差
frame_errors = []
for i in range(pred_len):
    error = torch.sqrt(
        (pred_traj[i, 0] - gt_traj[i, 0]) ** 2 +
        (pred_traj[i, 1] - gt_traj[i, 1]) ** 2
    )
    frame_errors.append(error.item())
    print(f"Frame {i} ({i*0.5:.1f}s): {error:.2f}m")

# 绘制误差增长曲线
plt.plot(frame_errors, 'o-')
plt.xlabel('Frame')
plt.ylabel('Error (meters)')
plt.title('Per-Frame Error Growth')
plt.grid(True)
plt.savefig('error_growth.png')
```

---

## 改进建议

### 短期改进（立即可行）

#### 1. 数据微调 ⭐⭐⭐⭐⭐
```bash
# 在SUScape训练集上fine-tune模型
python adzoo/orion/train.py \
    adzoo/orion/configs/suscape_finetune.py \
    --load-from /path/to/nuscenes_checkpoint.pth \
    --work-dir work_dirs/suscape_finetune
```

**预期效果**: L2误差可能降低50-70%

#### 2. 检查数据预处理 ⭐⭐⭐⭐
- 确认坐标系转换正确
- 检查数据归一化参数
- 验证时间采样率

#### 3. 调整超参数 ⭐⭐⭐
- 学习率
- 损失函数权重
- 预测长度

### 中期优化

#### 4. 改进损失函数 ⭐⭐⭐⭐
```python
# 当前可能只用L2 loss
# 可以添加：
- 速度匹配损失
- 加速度平滑损失
- 曲率一致性损失
- 不确定性建模
```

#### 5. 多模态预测 ⭐⭐⭐⭐
```
当前: 单一轨迹预测
改进: 预测多条可能轨迹（如K=6）
     选择最接近GT的轨迹计算误差
```

#### 6. 时序建模增强 ⭐⭐⭐
- 使用更强的时序模型（如Transformer）
- 增加历史轨迹的影响权重
- 引入运动学约束

### 长期改进

#### 7. 端到端优化 ⭐⭐⭐⭐⭐
- 联合优化感知和规划模块
- 减少误差累积
- 提高整体一致性

#### 8. 场景理解增强 ⭐⭐⭐⭐
- 更好的地图理解
- 交通规则约束
- 多车交互建模

#### 9. 不确定性估计 ⭐⭐⭐
- 预测轨迹的置信度
- 动态调整预测策略
- 提供可靠性评估

---

## 诊断步骤

### 第1步：检查数据质量

```python
# 检查GT轨迹是否合理
print("GT trajectory statistics:")
print(f"  Mean displacement: {gt_traj.mean():.2f}m")
print(f"  Max displacement: {gt_traj.max():.2f}m")
print(f"  Std displacement: {gt_traj.std():.2f}m")

# 检查预测轨迹
print("Predicted trajectory statistics:")
print(f"  Mean displacement: {pred_traj.mean():.2f}m")
print(f"  Max displacement: {pred_traj.max():.2f}m")
print(f"  Std displacement: {pred_traj.std():.2f}m")
```

### 第2步：可视化样本

随机选择一些样本可视化，检查：
- 预测是否完全偏离
- 是否有系统性偏差
- 是否是特定场景（如转弯）表现差

### 第3步：对比baseline

使用简单的baseline方法：
```python
# 常速直线运动baseline
baseline_traj = current_velocity * time_steps

# 计算baseline的L2误差
baseline_error = compute_L2(baseline_traj, gt_traj)

# 对比
if model_error < baseline_error:
    print("模型优于简单baseline")
else:
    print("模型可能有问题！")
```

---

## 总结

### L2误差的含义

- **6.32米（1s）**: 平均每个预测点与真实点相差6.32米
- **29.47米（3s）**: 3秒后的预测平均偏离真实位置29.47米

### 主要问题

1. ⭐⭐⭐⭐⭐ **域迁移**: nuScenes → SUScape
2. ⭐⭐⭐⭐⭐ **缺少微调**: 直接用预训练权重评估
3. ⭐⭐⭐⭐ **误差累积**: 长期预测误差指数增长
4. ⭐⭐⭐ **数据处理**: 可能存在预处理问题

### 最关键的改进

**立即执行**:
1. 在SUScape训练集上fine-tune模型
2. 检查数据预处理和坐标转换
3. 可视化一些样本确认问题

**预期结果**:
- Fine-tune后，L2误差应该能降低到：
  - 1s: 1-2米（改进75-85%）
  - 3s: 5-10米（改进65-80%）

### 评估标准

| 时段 | 优秀 | 良好 | 可接受 | 需改进 | 您的结果 |
|------|------|------|--------|--------|---------|
| 1s | <1m | 1-2m | 2-4m | >4m | 6.32m ⚠️ |
| 2s | <2m | 2-4m | 4-8m | >8m | 15.72m ⚠️ |
| 3s | <4m | 4-8m | 8-15m | >15m | 29.47m ⚠️ |

**结论**: 当前性能处于"需改进"级别，主要是因为**域迁移和缺少微调**。

---

## 参考文献

1. **ADE/FDE指标定义**: 
   - ADE (Average Displacement Error): 所有时间步的平均位置误差
   - FDE (Final Displacement Error): 最后时间步的位置误差

2. **STP3论文**: 
   - 提出了这套评估指标
   - 包括L2、碰撞率等

3. **nuScenes Benchmark**:
   - 标准的自动驾驶预测基准
   - 提供了性能对比参考

---

**文档创建时间**: 2026-01-29  
**版本**: 1.0  
**状态**: ✅ 完整分析
