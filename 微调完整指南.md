# SUScape数据集微调完整指南

> **ORION + ShareGPT QA 集成微调教程**

本指南将指导您如何使用SUScape数据集和ShareGPT问答数据对ORION模型进行微调。

---

## 📋 目录

1. [准备工作](#1-准备工作)
2. [数据准备](#2-数据准备)
3. [配置说明](#3-配置说明)
4. [开始微调](#4-开始微调)
5. [监控训练](#5-监控训练)
6. [评估模型](#6-评估模型)
7. [常见问题](#7-常见问题)

---

## 1. 准备工作

### 1.1 环境要求

```bash
# Python 3.8+
# PyTorch 1.10+
# CUDA 11.3+
# 建议: 4x A100 GPU (40GB显存)
```

### 1.2 检查数据文件

确认以下文件/目录存在：

```bash
# 数据目录
/lab/haoq_lab/cse12311753/suscape_scenes/          # SUScape场景数据
/lab/haoq_lab/cse12311753/suscape_scene_traj_csv_alldistance_fixyaw/  # CSV轨迹
/lab/haoq_lab/cse12311753/sharegpt_dataset/        # ShareGPT QA数据

# 标注文件
data/suscape_infos/suscape_infos_test.pkl          # 数据集索引

# 预训练模型
ckpts/orion_stage3.pth                             # ORION Stage 3预训练权重
ckpts/pretrain_qformer/                            # QFormer预训练权重
```

### 1.3 验证数据加载

运行验证脚本确保所有组件正常工作：

```bash
python validate_collision_detection.py
```

**期望输出**：
```
✓ PASS: CSV Structure
✓ PASS: Dataset Integration
✓ PASS: BEV Rendering
✓ ALL VALIDATIONS PASSED
```

---

## 2. 数据准备

### 2.1 分割数据集

将 `suscape_infos_test.pkl` 分割为训练集和验证集：

```bash
python tools/split_suscape_data.py \
    --input data/suscape_infos/suscape_infos_test.pkl \
    --output-dir data/suscape_infos \
    --train-ratio 0.8 \
    --seed 42
```

**输出**：
```
Total samples: 29040
Train samples: 23232 (80.0%)
Val samples: 5808 (20.0%)

创建文件:
  - data/suscape_infos/suscape_infos_train.pkl
  - data/suscape_infos/suscape_infos_val.pkl
```

### 2.2 验证分割结果

```bash
# 检查文件是否创建成功
ls -lh data/suscape_infos/

# 应该看到:
# suscape_infos_test.pkl   (原始文件)
# suscape_infos_train.pkl  (训练集)
# suscape_infos_val.pkl    (验证集)
```

---

## 3. 配置说明

### 3.1 微调配置文件

配置文件位置: `adzoo/orion/configs/suscape_finetune.py`

**关键配置项**：

```python
# 数据路径
data_root = "/lab/haoq_lab/cse12311753/suscape_scenes/"
csv_root = "/lab/haoq_lab/cse12311753/suscape_scene_traj_csv_alldistance_fixyaw/"
qa_root = "/lab/haoq_lab/cse12311753/sharegpt_dataset/"

# 训练参数
num_gpus = 4              # GPU数量
batch_size = 2            # 每GPU batch size
num_epochs = 10           # 训练轮数
lr = 1e-5                 # 学习率 (比预训练低10倍)

# QA集成
mix_qa_training = True    # 启用QA训练
use_critical_qa = True    # 使用QA作为LLM上下文
qa_tasks = ["q1", "q2", "q3", "q4", "q5", "q6", "q7", "q8", "q9"]

# 预训练模型
load_from = 'ckpts/orion_stage3.pth'  # 从Stage 3开始
```

### 3.2 自定义配置（可选）

如果需要修改训练参数，编辑 `suscape_finetune.py`:

```python
# 调整学习率
lr = 5e-6  # 更保守的学习率

# 调整训练轮数
num_epochs = 20  # 更多轮次

# 调整batch size
batch_size = 1  # 减小batch size以节省显存
```

---

## 4. 开始微调

### 4.1 方法1: 使用自动化脚本（推荐）

```bash
# 赋予执行权限
chmod +x run_suscape_finetune.sh

# 运行微调
./run_suscape_finetune.sh
```

**脚本会自动完成**：
1. 检查并创建train/val分割
2. 启动分布式训练
3. 保存checkpoints和日志

### 4.2 方法2: 手动运行

#### 多GPU训练（4个GPU）：

```bash
./adzoo/orion/orion_dist_train.sh \
    adzoo/orion/configs/suscape_finetune.py \
    4 \
    --work-dir work_dirs/orion_suscape_finetune \
    --load-from ckpts/orion_stage3.pth \
    --seed 42
```

#### 单GPU训练：

```bash
python adzoo/orion/train.py \
    adzoo/orion/configs/suscape_finetune.py \
    --work-dir work_dirs/orion_suscape_finetune \
    --load-from ckpts/orion_stage3.pth \
    --seed 42
```

### 4.3 使用SLURM提交任务（集群环境）

```bash
#!/bin/bash
#SBATCH --job-name=orion_finetune
#SBATCH --nodes=1
#SBATCH --ntasks-per-node=4
#SBATCH --gres=gpu:4
#SBATCH --time=48:00:00
#SBATCH --output=logs/finetune_%j.out

# 激活环境
source activate suscape

# 运行训练
./run_suscape_finetune.sh
```

---

## 5. 监控训练

### 5.1 实时日志

训练日志保存在:
```
work_dirs/orion_suscape_finetune/logs/
```

查看实时日志:
```bash
tail -f work_dirs/orion_suscape_finetune/logs/train.*.log
```

### 5.2 TensorBoard

启动TensorBoard:
```bash
tensorboard --logdir work_dirs/orion_suscape_finetune
```

访问: `http://localhost:6006`

### 5.3 训练输出示例

**正常的训练输出**：

```
Epoch [1][100/1000] lr: 1.000e-05, loss: 2.345, loss_cls: 0.234, loss_bbox: 0.456
[DEBUG] Added 8 QA conversations to input_dict for scene-000001
✓ Using 4 pre-annotated QA pairs from ShareGPT dataset

Epoch [1][200/1000] lr: 1.000e-05, loss: 2.123, loss_cls: 0.212, loss_bbox: 0.421
Epoch [1][300/1000] lr: 1.000e-05, loss: 1.987, loss_cls: 0.198, loss_bbox: 0.389

...

Epoch [10][1000/1000] lr: 1.000e-06, loss: 0.856, loss_cls: 0.085, loss_bbox: 0.156
Saving checkpoint to work_dirs/orion_suscape_finetune/epoch_10.pth
```

### 5.4 关键指标

监控以下指标：
- **Loss**: 应该逐渐下降（2.5 → 0.8）
- **Learning Rate**: 余弦退火（1e-5 → 1e-6）
- **QA Loading**: 每个样本应显示"✓ Using X pre-annotated QA pairs"
- **Memory**: GPU显存使用应稳定

---

## 6. 评估模型

### 6.1 评估最新checkpoint

```bash
python adzoo/orion/test.py \
    adzoo/orion/configs/suscape_eval.py \
    work_dirs/orion_suscape_finetune/latest.pth \
    --eval bbox
```

### 6.2 评估特定epoch

```bash
python adzoo/orion/test.py \
    adzoo/orion/configs/suscape_eval.py \
    work_dirs/orion_suscape_finetune/epoch_10.pth \
    --eval bbox
```

### 6.3 期望的评估指标

**微调前（使用预训练模型）**：
```
Collision Rate: 0.0% (无碰撞检测)
L2 Error (1s): ~3.5m
L2 Error (2s): ~6.2m
L2 Error (3s): ~9.1m
```

**微调后（期望改进）**：
```
Collision Rate: 15-25% (正常)
L2 Error (1s): ~1.2m  (改进 66%)
L2 Error (2s): ~2.4m  (改进 61%)
L2 Error (3s): ~3.8m  (改进 58%)

mAP: 提升 10-20%
NDS: 提升 5-15%
```

### 6.4 对比评估

```bash
# 评估预训练模型（baseline）
python adzoo/orion/test.py \
    adzoo/orion/configs/suscape_eval.py \
    ckpts/orion_stage3.pth \
    --eval bbox

# 评估微调模型
python adzoo/orion/test.py \
    adzoo/orion/configs/suscape_eval.py \
    work_dirs/orion_suscape_finetune/latest.pth \
    --eval bbox

# 对比结果
```

---

## 7. 常见问题

### Q1: 训练时显示 "scene_csv_data attribute not found"

**原因**: CSV数据未正确加载

**解决**:
```bash
# 检查csv_root配置
# 在 suscape_finetune.py 中确认:
csv_root = "/lab/haoq_lab/cse12311753/suscape_scene_traj_csv_alldistance_fixyaw/"

# 验证CSV文件存在
ls /lab/haoq_lab/cse12311753/suscape_scene_traj_csv_alldistance_fixyaw/*.csv
```

### Q2: OOM (Out of Memory) 错误

**解决方法**:

1. **减小batch size**:
```python
# 在 suscape_finetune.py 中
batch_size = 1  # 从2改为1
```

2. **使用gradient accumulation**:
```python
# 在配置中添加
optimizer_config = dict(
    type='Fp16OptimizerHook',
    loss_scale='dynamic',
    grad_clip=dict(max_norm=35, norm_type=2),
    cumulative_iters=2,  # 累积2个iter再更新
)
```

3. **减少workers**:
```python
data = dict(
    workers_per_gpu=2,  # 从4改为2
)
```

### Q3: QA数据未加载（"✗ No qa_conversations"）

**检查**:
```bash
# 确认QA文件存在
ls /lab/haoq_lab/cse12311753/sharegpt_dataset/suscape_NQA_q*.json

# 应该看到 q1.json 到 q9.json
```

**解决**:
```python
# 确认配置正确
qa_root = "/lab/haoq_lab/cse12311753/sharegpt_dataset/"
qa_tasks = ["q1", "q2", "q3", "q4", "q5", "q6", "q7", "q8", "q9"]
```

### Q4: 碰撞率仍然为0%

**诊断**:
```bash
python validate_collision_detection.py
```

如果显示 "Non-zero mask values: 0/120"，检查:

1. CSV文件路径正确
2. `_extract_agent_future_trajectories()` 的调试日志
3. 时间戳匹配是否成功

### Q5: 训练速度很慢

**优化建议**:

1. **启用混合精度训练** (已默认启用):
```python
optimizer_config = dict(
    type='Fp16OptimizerHook',
    loss_scale='dynamic',
)
```

2. **增加workers**:
```python
data = dict(
    workers_per_gpu=8,  # 从4增加到8
)
```

3. **使用Flash Attention** (已默认启用):
```python
flash_attn=True
```

### Q6: 如何恢复训练（从checkpoint继续）

```bash
./adzoo/orion/orion_dist_train.sh \
    adzoo/orion/configs/suscape_finetune.py \
    4 \
    --resume-from work_dirs/orion_suscape_finetune/latest.pth
```

### Q7: 如何调整训练轮数

编辑 `suscape_finetune.py`:
```python
num_epochs = 20  # 从10改为20

runner = dict(
    type='IterBasedRunner',
    max_iters=num_epochs * num_iters_per_epoch)
```

---

## 8. 微调最佳实践

### 8.1 学习率选择

- **保守**: `lr = 5e-6` (推荐用于小数据集)
- **标准**: `lr = 1e-5` (默认，适合大多数情况)
- **激进**: `lr = 2e-5` (快速收敛，但可能过拟合)

### 8.2 训练轮数

- **快速验证**: 3-5 epochs
- **标准训练**: 10 epochs (默认)
- **充分训练**: 20-30 epochs

### 8.3 数据增强

配置文件中已包含:
- 随机resize: (0.37, 0.45)
- 光度失真
- 归一化

可以调整 `ida_aug_conf` 来修改增强强度。

### 8.4 Checkpoint保存策略

```python
checkpoint_config = dict(
    interval=num_iters_per_epoch,  # 每个epoch保存
    max_keep_ckpts=3                # 只保留最近3个
)
```

---

## 9. 完整流程示例

```bash
# 1. 激活环境
source activate suscape

# 2. 验证数据
python validate_collision_detection.py

# 3. 分割数据（如果未分割）
python tools/split_suscape_data.py \
    --input data/suscape_infos/suscape_infos_test.pkl \
    --output-dir data/suscape_infos \
    --train-ratio 0.8

# 4. 开始微调
./run_suscape_finetune.sh

# 5. 监控训练（另一个终端）
tail -f work_dirs/orion_suscape_finetune/logs/train.*.log

# 6. 评估结果
python adzoo/orion/test.py \
    adzoo/orion/configs/suscape_eval.py \
    work_dirs/orion_suscape_finetune/latest.pth \
    --eval bbox
```

---

## 10. 输出文件说明

训练完成后，`work_dirs/orion_suscape_finetune/` 将包含:

```
work_dirs/orion_suscape_finetune/
├── logs/
│   └── train.*.log          # 训练日志
├── tf_logs/                 # TensorBoard日志
├── suscape_finetune.py      # 配置文件副本
├── epoch_1.pth              # Epoch 1 checkpoint
├── epoch_2.pth              # Epoch 2 checkpoint
├── ...
├── epoch_10.pth             # Epoch 10 checkpoint
├── latest.pth               # 最新checkpoint（软链接）
└── results_suscape_finetune/  # 可视化结果（如果启用）
```

---

## 11. 下一步

微调完成后，您可以：

1. **部署模型**: 使用微调后的模型进行在线推理
2. **进一步优化**: 尝试不同的超参数组合
3. **评估泛化性**: 在其他场景/数据集上测试
4. **可视化结果**: 生成预测轨迹的可视化

---

## 12. 获取帮助

如果遇到问题：

1. **查看文档**:
   - `COLLISION_DETECTION_FIX_SUMMARY.md`
   - `DEBUGGING_ZERO_MASKS.md`
   - `FIX_SCENE_CSV_DATA_NOT_LOADED.md`
   - `最终验证报告.md`

2. **运行验证脚本**:
   ```bash
   python validate_collision_detection.py
   python debug_future_trajectories.py
   ```

3. **检查日志**: 训练日志包含详细的调试信息

---

## 总结

本指南涵盖了SUScape数据集微调的完整流程。按照步骤操作，您应该能够:

✅ 准备训练数据  
✅ 配置微调参数  
✅ 启动分布式训练  
✅ 监控训练进度  
✅ 评估微调效果  
✅ 解决常见问题  

**预期改进**:
- L2误差减少 ~60%
- mAP提升 10-20%
- 碰撞检测正常工作（15-25%碰撞率）

祝您微调顺利！🚀

---

*最后更新: 2026-01-27*
