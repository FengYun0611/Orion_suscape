# 使用自定义检查点指南

## 概述

本指南说明如何使用您的自定义预训练检查点进行ORION模型微调。

**您的检查点路径**:
```
/lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth
```

---

## 方法1: 命令行直接指定（最推荐）✅

这是**最简单、最灵活**的方法。

### 单GPU训练

```bash
python adzoo/orion/train.py \
    adzoo/orion/configs/suscape_finetune.py \
    --load-from /lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth \
    --work-dir work_dirs/orion_suscape_finetune
```

### 多GPU训练（推荐4个GPU）

```bash
./adzoo/orion/orion_dist_train.sh \
    adzoo/orion/configs/suscape_finetune.py \
    4 \
    --load-from /lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth \
    --work-dir work_dirs/orion_suscape_finetune
```

### 优点

- ✅ **无需修改任何文件** - 保持代码干净
- ✅ **灵活切换** - 可以随时测试不同的检查点
- ✅ **清晰明确** - 命令中直接看到使用哪个检查点
- ✅ **便于记录** - 容易在脚本/日志中追踪

---

## 方法2: 修改 run_suscape_finetune.sh

### 修改步骤

编辑 `run_suscape_finetune.sh` 文件的第10行:

**修改前**:
```bash
PRETRAINED_CKPT="ckpts/orion_stage3.pth"
```

**修改后**:
```bash
PRETRAINED_CKPT="/lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth"
```

### 运行训练

修改后直接运行:
```bash
./run_suscape_finetune.sh
```

### 优点

- ✅ **一次修改** - 多次使用
- ✅ **自动处理** - 脚本自动处理数据分割
- ✅ **简单易用** - 一个命令完成所有步骤

---

## 方法3: 使用环境变量

### 设置环境变量

```bash
export PRETRAINED_CKPT="/lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth"
./run_suscape_finetune.sh
```

### 或者修改脚本支持环境变量

在 `run_suscape_finetune.sh` 第10行改为:
```bash
PRETRAINED_CKPT="${PRETRAINED_CKPT:-ckpts/orion_stage3.pth}"
```

然后可以通过环境变量覆盖:
```bash
PRETRAINED_CKPT="/lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth" ./run_suscape_finetune.sh
```

### 优点

- ✅ **不修改文件** - 保持脚本原样
- ✅ **会话级配置** - 可以在不同会话中设置不同值
- ✅ **便于批处理** - 适合在批处理脚本中使用

---

## 方法4: 修改配置文件

### 修改配置

编辑 `adzoo/orion/configs/suscape_finetune.py`，在文件**末尾**添加:

```python
# 自定义预训练检查点
load_from = '/lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth'
```

### 运行训练

```bash
python adzoo/orion/train.py \
    adzoo/orion/configs/suscape_finetune.py \
    --work-dir work_dirs/orion_suscape_finetune
```

### 优点

- ✅ **配置集中** - 所有配置在一个文件中
- ✅ **版本控制** - 可以提交到git
- ✅ **团队协作** - 便于团队共享配置

---

## 方法5: 使用 快速开始微调.sh

### 交互式运行

```bash
./快速开始微调.sh
```

脚本会：
1. 检查环境和数据
2. 询问检查点路径（输入您的路径）
3. 自动配置和启动训练

### 优点

- ✅ **用户友好** - 交互式提示
- ✅ **自动检查** - 验证所有依赖
- ✅ **适合新手** - 带有详细说明

---

## 完整训练流程示例

使用您的检查点进行完整训练:

### 步骤1: 验证检查点文件存在

```bash
ls -lh /lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth
```

预期输出:
```
-rw-r--r-- 1 user group 1.2G Jan 28 10:00 /lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth
```

### 步骤2: 准备训练数据（如果需要）

```bash
python tools/split_suscape_data.py \
    --input data/suscape_infos/suscape_infos_test.pkl \
    --output-dir data/suscape_infos \
    --train-ratio 0.8 \
    --seed 42
```

### 步骤3: 开始训练

```bash
./adzoo/orion/orion_dist_train.sh \
    adzoo/orion/configs/suscape_finetune.py \
    4 \
    --load-from /lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth \
    --work-dir work_dirs/orion_suscape_finetune \
    --seed 42
```

### 步骤4: 监控训练

查看日志:
```bash
tail -f work_dirs/orion_suscape_finetune/$(ls -t work_dirs/orion_suscape_finetune/*.log | head -1)
```

使用TensorBoard:
```bash
tensorboard --logdir work_dirs/orion_suscape_finetune
```

### 步骤5: 评估模型

```bash
python adzoo/orion/test.py \
    adzoo/orion/configs/suscape_eval.py \
    work_dirs/orion_suscape_finetune/latest.pth \
    --eval bbox
```

---

## 验证检查点加载

### 检查训练日志

成功加载检查点时，日志中应该显示:

```
2026-01-28 23:00:00,000 - mmdet - INFO - Loading checkpoint from /lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth
2026-01-28 23:00:02,000 - mmdet - INFO - Load checkpoint from: /lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth
2026-01-28 23:00:03,000 - mmdet - INFO - Loaded checkpoint from epoch 80, iteration 12345
2026-01-28 23:00:05,000 - mmdet - INFO - Loading tokenizer from /full/path/ckpts/pretrain_qformer/
2026-01-28 23:00:06,000 - mmdet - INFO - Tokenizer loaded successfully
2026-01-28 23:00:07,000 - mmdet - INFO - Start training...
```

### 关键信息

✅ **检查点路径正确** - 显示您的完整路径  
✅ **加载成功** - 显示epoch和iteration信息  
✅ **Tokenizer加载** - 从正确路径加载  
✅ **训练开始** - 开始训练过程

---

## 常见问题

### Q1: 检查点文件不存在

**错误**:
```
FileNotFoundError: [Errno 2] No such file or directory: '/lab/haoq_lab/.../Orion.pth'
```

**解决**:
```bash
# 检查文件是否存在
ls -lh /lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth

# 检查目录权限
ls -la /lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/

# 确认路径拼写正确（注意大小写）
```

### Q2: 检查点格式不匹配

**错误**:
```
KeyError: 'state_dict' not found in checkpoint
```

**解决**:
1. 确认检查点是ORION模型的检查点
2. 检查检查点版本是否兼容
3. 尝试使用不同的检查点

### Q3: --load-from 参数不识别

**错误**:
```
train.py: error: unrecognized arguments: --load-from
```

**解决**:
这个问题已在 commit #47 修复。确保使用最新代码:
```bash
git pull origin copilot/modify-program-for-evaluation
```

### Q4: Tokenizer 加载错误

**错误**:
```
HFValidationError: Repo id must be in the form 'repo_name' or 'namespace/repo_name'
```

**解决**:
这个问题已在 commit #52 修复。确保使用最新代码。

### Q5: 路径太长，可以用相对路径吗？

**可以！** 创建软链接:
```bash
# 在项目根目录创建软链接
ln -s /lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts ./my_ckpts

# 然后使用相对路径
--load-from my_ckpts/Orion.pth
```

---

## 路径对比

### 默认路径（相对路径）

```bash
ckpts/orion_stage3.pth
./ckpts/orion_stage3.pth
```

### 您的路径（绝对路径）

```bash
/lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth
```

### 两者都能正常工作

- ✅ **相对路径**: 会自动转换为绝对路径
- ✅ **绝对路径**: 直接使用
- ✅ **所有路径问题已修复** (commits #48-52)

---

## 同时测试多个检查点

### 运行多个实验

```bash
# 实验1: 使用您的检查点
./adzoo/orion/orion_dist_train.sh \
    adzoo/orion/configs/suscape_finetune.py \
    4 \
    --load-from /lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth \
    --work-dir work_dirs/exp_custom_ckpt

# 实验2: 使用默认检查点
./adzoo/orion/orion_dist_train.sh \
    adzoo/orion/configs/suscape_finetune.py \
    4 \
    --load-from ckpts/orion_stage3.pth \
    --work-dir work_dirs/exp_default_ckpt

# 实验3: 使用另一个检查点
./adzoo/orion/orion_dist_train.sh \
    adzoo/orion/configs/suscape_finetune.py \
    4 \
    --load-from /path/to/another_checkpoint.pth \
    --work-dir work_dirs/exp_another_ckpt
```

### 对比结果

```bash
# 查看各个实验的评估结果
python adzoo/orion/test.py adzoo/orion/configs/suscape_eval.py work_dirs/exp_custom_ckpt/latest.pth --eval bbox
python adzoo/orion/test.py adzoo/orion/configs/suscape_eval.py work_dirs/exp_default_ckpt/latest.pth --eval bbox
python adzoo/orion/test.py adzoo/orion/configs/suscape_eval.py work_dirs/exp_another_ckpt/latest.pth --eval bbox
```

---

## 总结和建议

### 推荐方法

**对于您的场景，推荐使用方法1（命令行直接指定）**:

```bash
./adzoo/orion/orion_dist_train.sh \
    adzoo/orion/configs/suscape_finetune.py \
    4 \
    --load-from /lab/haoq_lab/cse12311753/VLA/Orion_2/Orion/ckpts/Orion.pth \
    --work-dir work_dirs/orion_suscape_finetune
```

### 为什么推荐这个方式？

1. ✅ **无需修改文件** - 保持代码库干净
2. ✅ **灵活切换** - 可以轻松测试不同检查点
3. ✅ **清晰明确** - 命令中直接看到使用的检查点
4. ✅ **便于记录** - 容易在脚本和日志中追踪
5. ✅ **团队协作** - 每个人可以使用自己的检查点路径

### 关键点

- ✅ `--load-from` 参数已支持（commit #47）
- ✅ Tokenizer 路径问题已修复（commit #52）
- ✅ 所有路径格式都能正常工作
- ✅ 文档完整，立即可用

### 下一步

1. **验证检查点**: 确认文件存在并可访问
2. **准备数据**: 运行数据分割脚本
3. **开始训练**: 使用推荐的命令
4. **监控进度**: 查看日志和TensorBoard
5. **评估结果**: 训练完成后评估模型

---

## 相关文档

- **微调完整指南.md** - 完整的微调教程
- **训练参数修复说明.md** - `--load-from` 参数详解
- **Tokenizer加载错误最终修复.md** - Tokenizer问题解决
- **README_微调.md** - 快速入门指南
- **TRAINING_ARGUMENTS.md** - 英文训练参数文档

---

## 获取帮助

如果遇到问题：

1. 检查 **常见问题** 部分
2. 查看相关文档
3. 检查训练日志
4. 运行验证脚本: `python validate_collision_detection.py`

---

**文档版本**: 1.0  
**最后更新**: 2026-01-28  
**状态**: ✅ 完整、可用

**现在您可以使用您的检查点开始训练了！** 🚀
